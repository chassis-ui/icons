---
import type { CollectionEntry } from 'astro:content'
import Icon from '@chassis-ui/docs/components/shortcodes/Icon.astro'

interface Props {
  icon: CollectionEntry<'icons'>['data']
}

const { icon } = Astro.props
---

<h2 class="font-strong">Examples</h2>
<div class="p-medium p-md-large border rounded icon-demo-examples">
  <div class="h1 align-items-center">
    <Icon name={icon.title} class="icon-adaptive" /> Heading
  </div>
  <div class="h3">
    <Icon name={icon.title} class="icon-adaptive" /> Smaller heading
  </div>
  <p>
    Inline text <Icon name={icon.title} class="icon-adaptive" />
  </p>
  <p>
    <a href="#here" class="text-decoration-none">
      Example link text <Icon name={icon.title} class="icon-adaptive" />
    </a>
  </p>
  <div class="d-flex gap-small mb-medium">
    <button type="button" class="button default">
      <Icon name={icon.title} />
      <span>Button</span>
    </button>
    <button type="button" class="button primary">
      <Icon name={icon.title} />
      <span>Button</span>
    </button>
    <button type="button" class="button primary outline">
      <Icon name={icon.title} />
      <span>Button</span>
    </button>
    <button type="button" class="button primary smooth">
      <Icon name={icon.title} />
      <span>Button</span>
    </button>
  </div>
  <div class="d-flex gap-small mb-medium">
    <button type="button" class="button default icon-only">
      <Icon name={icon.title} />
      <span class="visually-hidden">Button</span>
    </button>
    <button type="button" class="button secondary icon-only">
      <Icon name={icon.title} />
      <span class="visually-hidden">Button</span>
    </button>
    <div class="button-group">
      <button type="button" class="button default icon-only">
        <Icon name={icon.title} />
        <span class="visually-hidden">Button</span>
      </button>
      <button type="button" class="button default icon-only">
        <Icon name={icon.title} />
        <span class="visually-hidden">Button</span>
      </button>
      <button type="button" class="button default icon-only">
        <Icon name={icon.title} />
        <span class="visually-hidden">Button</span>
      </button>
    </div>
    <div class="button-group">
      <button type="button" class="button secondary icon-only">
        <Icon name={icon.title} />
        <span class="visually-hidden">Button</span>
      </button>
      <button type="button" class="button secondary icon-only">
        <Icon name={icon.title} />
        <span class="visually-hidden">Button</span>
      </button>
      <button type="button" class="button secondary icon-only">
        <Icon name={icon.title} />
        <span class="visually-hidden">Button</span>
      </button>
    </div>
  </div>
  <div class="mb-medium">
    <div class="input-group">
      <span class="input-addon" id="basic-addon1">
        <Icon name={icon.title} />
      </span>
      <input
        type="text"
        class="form-input"
        placeholder="Input group example"
        aria-label="Input group example"
        aria-describedby="basic-addon1"
      />
    </div>
  </div>
  <div class="notification success" role="alert">
    <Icon name={icon.title} class="notification-icon" />
    <p>An example notification with an icon!</p>
  </div>
</div>
